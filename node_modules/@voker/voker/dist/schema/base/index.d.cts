/**
 * Represents an agent version.
 *
 * See [Agent Versions](https://app.voker.ai/docs/concepts/agents#agent-versions) for more information.
 */
type AgentVersion = {
    /**
     * Unique identifier for the agent version within its agent.
     */
    agentVersionName: string;
    description: string | null;
    /**
     * Unique number assigned to the agent version within its agent.
     */
    agentVersionNumber: number;
    createdAt: Date;
    deprecatedAt: Date | null;
};
type RawAgentVersion = {
    agent_version_name: string;
    description: string | null;
    agent_version_number: number;
    created_at: string;
    deprecated_at: string | null;
};
declare const AgentVersionT: {
    deserialize: (data: RawAgentVersion) => AgentVersion;
};
declare class AgentVersionNotFoundError extends Error {
}

/**
 * Represents an agent.
 *
 * See [Agents](https://app.voker.ai/docs/concepts/agents#agents) for more information.
 */
type Agent = {
    /**
     * Unique identifier for the agent.
     */
    agentName: string;
    description: string | null;
    /**
     * Unique number assigned to the agent.
     */
    agentNumber: number;
    createdAt: Date;
    deprecatedAt: Date | null;
};
type RawAgent = {
    agent_name: string;
    description: string | null;
    agent_number: number;
    created_at: string;
    deprecated_at: string | null;
};
declare const AgentT: {
    deserialize: (data: RawAgent) => Agent;
};
declare class AgentNotFoundError extends Error {
}

/**
 * Represents a person.
 *
 * See [People](https://app.voker.ai/docs/concepts/people) for more information.
 */
type Person = {
    personId: string;
    properties: Record<string, any> | null;
    createdAt: Date;
};
type RawPerson = {
    person_id: string;
    properties: Record<string, any> | null;
    created_at: string;
};
declare const PersonT: {
    deserialize: (data: RawPerson) => Person;
};
declare class PersonNotFoundError extends Error {
}

/**
 * Represents a fingerprint.
 *
 * See [Fingerprints](https://app.voker.ai/docs/concepts/events#fingerprints) for more information.
 */
type Fingerprint = {
    fingerprintId: string;
};
type RawFingerprint = {
    fingerprint_id: string;
};
declare const FingerprintT: {
    deserialize: (data: RawFingerprint) => Fingerprint;
};
declare class FingerprintNotFoundError extends Error {
}

export { type Agent, AgentNotFoundError, AgentT, type AgentVersion, AgentVersionNotFoundError, AgentVersionT, type Fingerprint, FingerprintNotFoundError, FingerprintT, type Person, PersonNotFoundError, PersonT };
